export class Mesa {
  constructor(
    public idMesa: number,
    public Status_mesa: boolean,
    public Preco_total: number,
    public Numero_mesa: number,
    public Tipo_pagamento: string
  ) {}
}
